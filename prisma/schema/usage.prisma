model Usage {
  id Int @id @default(autoincrement())

  prompt   Int
  response Int

  model Models

  tickerProcessing TickerProcessing?

  aiProcessing   AiProcessing? @relation(fields: [aiProcessingId], references: [id])
  aiProcessingId Int?

  createdAt DateTime @default(now())

  @@map("usage")
}
